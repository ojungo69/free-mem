# Verify Tasks Report: 007-oboete-m1-alpha (Phase 2, T012-T046)

- Date: 2026-09-04
- Scope: all (origin/main..HEAD at 3cc1727, working tree clean)
- Tasks verified: 35 of 35 requested (T012-T046, marked [X] on 2026-09-04)
- Method: one independent verifier agent per task (Dynamic Workflow, opus), five layers each; this session implemented Phase 2, so the checks were not run by it

> ⚠️ **FRESH SESSION ADVISORY**: For maximum reliability, run `/speckit.verify-tasks` in a **separate** agent session from the one that performed `/speckit.implement`. The implementing agent's context biases it toward confirming its own work.

## Summary scorecard

| Verdict | Count |
|---|---|
| ✅ VERIFIED | 34 |
| 🔍 PARTIAL | 1 |
| ⚠️ WEAK | 0 |
| ❌ NOT_FOUND | 0 |
| ⏭️ SKIPPED | 0 |

## Flagged items

### T014 — 🔍 PARTIAL

CI matrix (22.16.0 / 24.x) with typecheck, lint, build, test exists in .github/workflows/ci.yml plus a coverage/lcov + Sonar job; the task line's fifth step, pack-check, is absent from CI and from package.json scripts — it is deferred to the still-open T087.

| Layer | Result |
|---|---|
| 1. File existence | positive |
| 2. Git diff cross-reference | positive |
| 3. Content pattern matching | negative |
| 4. Dead-code detection | not_applicable |
| 5. Semantic assessment | skipped |

⚠️ Interpretive: not run — layer 3 produced a negative, so the semantic pass was skipped per protocol. Mechanically the `engine` job is a real, connected matrix (fail-fast:false, node 22.16.0 / 24.x, npm ci + typecheck + lint + build + test), not a stub; the only shortfall is the missing pack-check step.

Evidence gaps:

- pack-check named in the T014 task line is not present in .github/workflows/ci.yml (no step) nor as an npm script in package.json:20-26; grep for 'pack-check' across the repo finds only docs and the open task specs/007-oboete-m1-alpha/tasks.md:263 (T087, unchecked). T012's own note also records 'no pack-check script', so the omission is tracked but T014 is marked [X] with that element unimplemented.
- Done note's caveat 'sonar.sources still scripts (T088)' verified accurate: sonar-project.properties:4 is `sonar.sources=scripts`, so the Sonar analysis in the `check` job does not cover src/.

## Verified items

| Task | Verdict | Summary |
|---|---|---|
| T012 | ✅ VERIFIED | package.json (name oboete, bin, engines >=22.16, build/typecheck/lint/test), tsconfig.json, eslint.config.js and populated src//test/ skeletons all present and coherent; two deviations are disclosed in the done note (files whitelist instead of .npmignore; pack-check deferred to T087). |
| T013 | ✅ VERIFIED | scripts/build.mjs exists, is in the diff, and actually produces dist/oboete.mjs plus 31 build/test files; bundling/external split and embedded SQL migrations confirmed by inspecting the built bundle. Viewer-asset embedding is explicitly deferred to the still-open T079. |
| T015 | ✅ VERIFIED | Both files exist, are in the diff, and contain a genuine measurement harness plus recorded R13 cold-start and installed-size rows (both pass). Noted deviations: v22.23.1 used as a documented proxy for 22.16, and hook paths judged at 300 ms rather than the task line's 100 ms. |
| T016 | ✅ VERIFIED | 0001_core.sql exists in the diff, defines all nine required tables including the seeded worker_lease row and the four extra sessions columns, and is applied by src/db/open.ts as migration version 1. |
| T017 | ✅ VERIFIED | 0002_memory_search.sql exists, is in the diff, and contains every named object (memories with material_hash/content_hash/review_state, memory_sources, memories_fts trigram, memories_fts_cjk, all six triggers, seeded destination_rules), registered as migration v2 in src/db/open.ts. |
| T018 | ✅ VERIFIED | 0003_operations.sql exists, is in the diff, defines injections (with context_epoch/attempts_json/delivery_count), injection_items with the partial unique index, provider_usage and sync_conflicts, and is registered as migration version 3 in src/db/open.ts. |
| T019 | ✅ VERIFIED | src/db/open.ts fully implements the timeout/WAL/wal_autocheckpoint=0 hook mode, the forward-only sha256-checked migration runner, and the schemaBehind signal that routes hook writes to the spool; all exports are referenced by src/ and test/. |
| T020 | ✅ VERIFIED | Migration smoke tests exist and are real: test/migrations/apply.test.ts covers the empty DB and the version-1 fixture (plus hook/mismatch/ahead/busy paths), the fixture is committed at user_version 1, and CI runs the file on Node 22.16.0 and 24.x. 8/8 pass locally. |
| T021 | ✅ VERIFIED | identity.ts (materialHash/contentHash/memoryIdFor/normalizeForIdentity) and queries.ts memoryScope are fully implemented, live-read destination_rules, fail closed, and are called from injection/observer/worker; queries.test.ts covers cross-repo, secret, and imported exclusion. |
| T022 | ✅ VERIFIED | All three named files exist, are in the diff, and carry every named symbol. No dead symbols (all exports referenced from src/ or test/). Semantics genuinely implemented: OBOETE_HOME, smol-toml config, .oboete.toml bounds, OBOETE_*-only credentials, 5-field consent hash, pause check before DB open. |
| T023 | ✅ VERIFIED | repo-identity.ts implements normalized-remote or git-common-dir realpath identity with sha256 16-hex id; both files in the diff, symbols consumed by capture.ts and inject.ts, tests cover the credential remote and two-call bound. |
| T024 | ✅ VERIFIED | T024's test file and secrets corpus both exist, are in the diff, and every task clause has a real assertion against the actual src/privacy modules. The stdin/1 MB clause is legitimately absent, deferred to T027 per the done note. |
| T025 | ✅ VERIFIED | src/privacy/detect.ts implements all named behaviours (private strip, compileGlob path rules, secretlint lintSource with static recommend preset, gated entropy, [REDACTED:<rule>], worker_threads cutoff, per-field redaction) and is wired into capture.ts, cli.ts, observe.ts with unit tests. |
| T026 | ✅ VERIFIED | src/events.ts implements the zod discriminated union, kind-in-every-key event id derivation with optional turnOrdinal, and the conversation-id policy table; all wired into src/capture.ts and covered by test/unit/events.test.ts. |
| T027 | ✅ VERIFIED | T027 fully implemented in src/capture.ts (1316 lines) with src/spool.ts and src/cli.ts wiring; deadline/reserve/busy-timeout/detector-first/spool-rename/lease-gated spawn all present and referenced from tests. Only noted deviation is the stdin bound, which the done note itself declares. |
| T028 | ✅ VERIFIED | All four agent adapters plus the shared adapt()/scanPartialPrefix entry points are implemented in src/agents/, wired into src/capture.ts, and covered by test/unit/agents.test.ts including the unknown-payload path-rule secret test and the escaped Windows path scan. |
| T029 | ✅ VERIFIED | egress.ts (destination_rules evaluation) and classify.ts (promoteSensitivity/strictest) are fully implemented, consumed by worker/observer code, and covered by test/unit/privacy.test.ts including an explicit SC-006 agent-invariance test. |
| T030 | ✅ VERIFIED | src/worker/lease.ts implements claim/heartbeat/release with BEGIN IMMEDIATE transactions, owner_token fencing, 6 s/60 s staleness and an empty-queue atomic release; all symbols are used by observe/classify/purge/capture and test/unit/lease.test.ts covers lease steal and clock jump. |
| T031 | ✅ VERIFIED | Both named files exist, are in the diff, and contain every claimed behaviour: spool recovery with INSERT OR IGNORE plus quarantine, per-(session, through, destination) batching, ten_turns/session_end/retention triggers, 120 s reclaim. All exports are wired into src/worker/observe.ts. |
| T032 | ✅ VERIFIED | src/worker/purge.ts and test/unit/purge.test.ts both exist, are in the diff, implement bounded lease-guarded deletes, pi-ack cleanup and PASSIVE/TRUNCATE checkpoints, and every export is consumed by observe.ts / deferred.ts / catalog.ts. |
| T033 | ✅ VERIFIED | src/observer/contract.ts implements all zod schemas and every cap named in T033, with lenient-parse -> trim -> strict-validate and excerptInput(12,000 chars); all exports are consumed by llm/providers/request/fallback/classify and covered by test/unit/contract.test.ts. |
| T034 | ✅ VERIFIED | Single outbound request builder exists, is wired into the observe worker, applies destination_rules to events/free_summaries/nearby, emits opaque repo_ref, and is covered by body-level tests. Consent re-check is implemented in llm.ts/observe.ts rather than request.ts. |
| T035 | ✅ VERIFIED | Both files exist, are in the diff, and contain every named preset, limit, retry/classification code, model-id check and neuron estimate. Symbols are imported by src/worker/observe.ts and exercised by unit tests. No stubs. |
| T036 | ✅ VERIFIED | Reservation/daily-cap logic lives in src/observer/reservation.ts (as the done note says, not observe.ts), is wired into src/worker/observe.ts and src/observer/llm.ts's 3036 handling, and is covered by test/unit/callpolicy.test.ts. All mechanical layers positive. |
| T037 | ✅ VERIFIED | src/observer/fallback.ts implements all four deterministic rules with per-rule source_event_ids, 20-observation budget and trim order, agent-neutral output; called from src/worker/observe.ts:324; test/unit/fallback.test.ts covers long paths, 60 calls, and copied-text language. |
| T038 | ✅ VERIFIED | classify.ts implements the fenced apply transaction with repo-scoped target restriction, tombstone suppression, add/update sensitivity maxing, reasoned delete and directive rejection; both named tests and the directive corpus exist and all exports are consumed by worker/observe.ts and injection. |
| T039 | ✅ VERIFIED | src/worker/observe.ts implements runObserve as a full fenced-lease worker loop with exit codes 0/1/3 per contracts/cli.md, wired into src/cli.ts and covered by 15 tests in test/unit/observe.test.ts including the required fully-private-session case. |
| T040 | ✅ VERIFIED | src/observer/catalog.ts implements refreshWorkersAiCatalog with a 24h runtime_state cache keyed to accountId and paidPlan from require_workers_paid; wired once-per-run in src/worker/observe.ts and covered by test/unit/catalog.test.ts. |
| T041 | ✅ VERIFIED | fts.ts/query.ts implement Intl.Segmenter routing, trigram >=3, CJK bigrams incl. ー々・, particle drop, LIKE-only fallback, and bm25-ordered FTS; tests cover EN+JA seeded rows. BM25 normalization itself lives in rank.ts (T042), as expected. |
| T042 | ✅ VERIFIED | src/retrieval/rank.ts implements normalized BM25 (0.3 threshold), RRF k=60 with LIKE never voting, MMR lambda 0.5 over character-trigram cosine, and a character-budget cut; consumed by injection/pack.ts and db/queries.ts, with 10 dedicated tests in test/unit/retrieval.test.ts. |
| T043 | ✅ VERIFIED | budget.ts implements min(channel cap, context_fraction x documented window) with windows parsed from docs/research/context-windows.md, window_unknown fallback and blocked lane; wired into inject.ts/pack.ts and covered by test/unit/budget.test.ts. |
| T044 | ✅ VERIFIED | staleness.ts and pack.ts both implement the described behaviour and are wired into inject.ts/deferred.ts; test/unit/pack.test.ts covers every named case. Done-note correction (1 s wait, not 8 s) matches SUMMARY_WAIT_MS = 1_000. |
| T045 | ✅ VERIFIED | T045 fully implemented: src/injection/deferred.ts contains the complete Grok deferred state machine (merge, attach, deliver, failure, stop) in BEGIN IMMEDIATE transactions, wired into src/injection/inject.ts, with all six named test cases present in test/unit/deferred.test.ts. |
| T046 | ✅ VERIFIED | T046 fully implemented: `oboete inject --agent pi --kind start/prompt` (runInject) plus Claude/Codex/Grok hook injection branches, 1,300 ms deadline, plain-stdout vs additionalContext rules, and directive gating via the observer's rejectsDirectives. All layers positive. |

## Unassessable items

None.


## Interpretive notes (layer 5)

- T012: Interpretive: package.json/tsconfig.json/eslint.config.js are real, non-stub configs consistent with plan.md "Source Code": name oboete, type module, bin oboete -> dist/oboete.mjs (actually produced by scripts/build.mjs:38 with chmod 0755), engines node >=22.16, exact-pinned deps, and build/typecheck/lint/test scripts that wire to node scripts/build.mjs, tsc --noEmit, eslint ., and node --test over build/test. tsconfig NodeNext + strict includes src/**/*.ts and test/**/*.ts; eslint config covers the same globs. src/ and test/ skeletons exist and are populated (src/cli.ts, config.ts, paths.ts, repo-identity.ts, events.ts, capture.ts, db/, privacy/, worker/, observer/, retrieval/, injection/, agents/; test/unit, migrations, contracts, fixtures, helpers, e2e-*.test.ts).
- T013: Interpretive: scripts/build.mjs really builds. Ran `npm run build`: it emitted dist/oboete.mjs (1.3MB, chmod 755, shebang + createRequire shim) and 31 compiled test files under build/test (unit/, migrations/, e2e-*). Verified inside the bundle: `CREATE TABLE` (migrations embedded via the `.sql` text loader, imported at src/db/open.ts:6-8), zod and smol-toml present, secretlint referenced 126x — i.e. hook-path packages are genuinely bundled. EXTERNAL keeps ai/@ai-sdk/*/workers-ai-provider/hono/@hono/*/preact out, and those are loaded lazily (src/observer/providers.ts:72,82; src/observer/llm.ts:390). Wired into package.json "build" and "test". Not a stub. One scope gap, honestly recorded: the task line's "viewer assets embedded" is only a placeholder comment (scripts/build.mjs:44 "Viewer assets (src/viewer/app, Vite) are embedded here from T079 on."); src/viewer does not exist yet and T079 (tasks.md:232) is still unchecked, so the deferral is consistent rather than a phantom claim.
- T015: Interpretive: the evidence doc records a real, reproducible measurement, not a placeholder. measure-cold-start.mjs (324 lines) spawns the built bundle under each given Node, runs 3 warm-ups + 30 measured runs per scenario (--version, hook small/clean-200KB/secret-dense-200KB with DB, hook with DB absent), verifies work actually landed (raw_events count / spool files), picks the lower-load of 2 attempts, and emits the exact markdown table pasted between the <!-- measure:start/end --> markers in docs/evidence/m1-resource-envelope.md:52-71. Both R13 rows are recorded with commands, machine, commit cfd66d3, and pass/fail per row (m1-resource-envelope.md:129-136). The installed-size re-measure (29.152 MB) is corroborated by package.json: preact, smol-toml and @secretlint/core are indeed devDependencies and only 7 runtime deps remain. Two documented deviations, reported as notes not defects: (a) 22.16 was measured with the local v22.23.1 as an explicit stated proxy for CI's pinned 22.16.0 (m1-resource-envelope.md:7); (b) the task's "100 ms" budget is applied to `--version` only — hook paths are judged against the capture contract's 300 ms deadline (max 252.1 ms), stated at m1-resource-envelope.md:46, so the 252 ms hook figure is not an unflagged budget breach.
- T016: Interpretive: All nine tables are created with STRICT typing, CHECK constraints and the indexes/uniques described in data-model.md (sessions carries conversation_id/context_epoch/last_compaction_key/summary_state; worker_lease has the seeded id=1 row via INSERT). The migration is not inert: src/db/open.ts:6,15 imports it as version 1 of the migration list and applies it with sha256 recorded into schema_migrations (open.ts:120,167). No stubs or TODOs.
- T017: Interpretive: the migration is genuinely implemented and wired, not a stub. src/db/open.ts:7,16 imports 0002_memory_search.sql and registers it as version 2 in MIGRATIONS, applied with a sha256 ledger in schema_migrations. Contents match data-model.md: memories carries material_hash (dm:108), content_hash TEXT NOT NULL UNIQUE (dm:109), review_state with the unreviewed/reviewed/imported CHECK and 'unreviewed' default (dm:111), and exactly the three indexes at dm:123. memory_sources (dm:125) has the citation_kind CHECK and a memory_id index with ON DELETE CASCADE. memories_fts uses tokenize='trigram' over (title, body) and memories_fts_cjk unicode61 over cjk_bigrams, both external-content FTS5 with content='memories' plus full ai/ad/au trigger sets (dm:135-138). The done note's "UPDATE OF the indexed columns" narrowing is visible: memories_fts_au fires on UPDATE OF title, body and memories_fts_cjk_au on UPDATE OF cjk_bigrams, so hot-path writes like markInjected/pin do not re-tokenize. destination_rules is seeded with 15 rows; the absent (sync, secret) row is intentional per data-model.md:152 ("sync (M2) | everything except secret"). Tables are consumed downstream (src/db/queries.ts, src/retrieval/query.ts, src/privacy/egress.ts, src/observer/classify.ts) and exercised by test/migrations/apply.test.ts.
- T018: Interpretive: 0003_operations.sql defines all four tables with real DDL (no stubs) and is wired into the migration runner — src/db/open.ts:8 imports it and registers `{ version: 3, name: '0003_operations', sql: sql0003 }` (line 17), so it is applied and hash-recorded like 0001/0002. Columns match data-model.md §injections (context_epoch line 166, attempts_json 167, delivery_count 168) and §injection_items (191: partial unique index on (conversation_id, context_epoch, memory_id) WHERE decision='included' AND memory_id IS NOT NULL — implemented verbatim as index injection_items_included_memory). The tables are consumed by application code (src/injection/inject.ts, ledger.ts, deferred.ts, src/worker/observe.ts, src/observer/reservation.ts), so the schema is connected, not orphaned. Note (not a defect): the done note's added reasons `secret_detected` and `directive` are present in the CHECK constraint but data-model.md line 187 still lists the older reason set — spec doc lags the consolidation the note describes.
- T019: Interpretive: src/db/open.ts implements every clause. `new DatabaseSync(path, { timeout: options.timeoutMs })` (line 72) is the explicit timeout; WAL + foreign_keys + synchronous pragmas at 74-76; the hook branch (79) sets `PRAGMA wal_autocheckpoint = 0`, reads user_version and returns `schemaBehind` without ever calling migrate() — matching the done note "hook role never migrates". migrate() (135) is forward-only (`if (migration.version <= userVersion) continue`), throws SchemaAheadError when user_version > 3, verifies each applied row's sha256 against `sha256Hex(bundled.sql)` and throws MigrationMismatchError (verifyAppliedHashes, 112-129), and applies each migration inside BEGIN IMMEDIATE with rollback on error. The "hook spools when user_version is behind" clause is realised at the call sites: src/capture.ts:790-792 closes the db and falls through to the spool write when `opened.schemaBehind`, and src/injection/inject.ts:578-581 closes and reports index-unavailable. No stubs, TODOs or hardcoded returns. test/migrations/apply.test.ts exercises fresh apply, idempotent re-open, schemaBehind=true for a hook on an older file, DatabaseMissingError, MigrationMismatchError, SchemaAheadError and isBusyError.
- T020: Interpretive: apply.test.ts holds 8 real assertions-bearing tests, not stubs — empty-DB path asserts schemaVersion 3, per-migration sha256 rows, integrity_check/WAL/foreign_keys; the fixture path copies test/fixtures/previous-version.db (verified PRAGMA user_version = 1, one schema_migrations row 0001_core), upgrades to 3, preserves the version-1 applied_at, and exercises FTS after upgrade; plus hook-role no-migrate, MigrationMismatchError, SchemaAheadError, and busy-retry cases. "Both Node versions" is satisfied through CI: .github/workflows/ci.yml matrix.node = ["22.16.0","24.x"] runs `npm test`, whose script explicitly globs build/test/migrations/**/*.test.mjs (package.json:24), so this file runs on both. Ran it locally: 8/8 pass.
- T021: Interpretive: memoryScope (src/db/queries.ts:71-100) genuinely builds the shared filter — repo_id, deleted_at IS NULL, valid_to IS NULL, review_state <> 'imported', and a sensitivity IN (...) list read live from the destination_rules table (not hardcoded), failing closed with '0' when no sensitivity is allowed, and throwing for destination 'sync' (M2). identity.ts implements NFKC/whitespace/case normalization then sha256Json for materialHash (type deliberately excluded) and contentHash(repoId, material). No stubs, TODOs or hardcoded returns. Both modules are consumed by real callers: src/injection/pack.ts:539,610, src/observer/classify.ts:9-10,264,272, src/worker/observe.ts:367, src/injection/inject.ts:12. test/unit/queries.test.ts covers the three claims (cross-repository exclusion, secret exclusion, imported quarantine) at lines 223, 242, 278. Note on scope wording, not a defect: the task line says the filter is used by "injection, CLI, MCP, and viewer" — only injection/observer consume it today; src/cli.ts contains no memory query at all (0 occurrences of "FROM memories", no import of db/queries), so there is no bypassing reader, and no src/mcp or viewer surface exists yet in this milestone. The done note's two claims both check out: memoryScope reads destination_rules live, and nearbyCandidates (queries.ts:273) is present with bm25+rrf and no threshold.
- T022: Interpretive: behaviour is real and wired, not stubbed. OBOETE_HOME resolution with resolve() for relative overrides (paths.ts:19-23) and a single path composer (paths.ts:26-40, incl. `paused` marker). config.ts parses config.toml with smol-toml (line 3, dep pinned package.json:46) under a zod strictObject, and assertNoCredentialKeys (config.ts:209) actively refuses credential-like keys in the file, so credentials come only from OBOETE_* env (readCredentials, config.ts:297-333: OBOETE_CF_API_TOKEN/OBOETE_CF_ACCOUNT_ID and credential.envName). consentHash (config.ts:360-368) hashes exactly the five named fields preset/host/credentialSource/costClass/egressClasses, and consentMatches (375-382) fails closed for remote presets with no stored record. PRESET_CATALOG (config.ts:46) contains no anthropic entry, matching the A19 done-note. Repo secret_paths bound to 64 rules x 256 chars via zod .max() (config.ts:146-158), matching review finding 14. isPaused is consumed at three real call sites before the DB is opened: capture.ts:865 (comment "the paused marker is read before stdin and before the database"; openDatabase used later at 789's helper), inject.ts:553 (before openDatabase at 569), observe.ts:654 (before openDatabase at 668). test/unit/config.test.ts has 21 tests covering every one of these behaviours including the preset-catalog/contract match and the repo-rule bounds.
- T023: Interpretive: src/repo-identity.ts:73-97 normalizes remotes by reading only hostname/port/pathname from URL (dropping userinfo, query, fragment) plus an scp-like branch; :114 derives the id as the first 16 hex of sha256 over the normalized identity, matching data-model.md. Fallback at :152 is realpath of the resolved git common dir; :141 fetches --show-toplevel and --git-common-dir in one call so an origin repo costs at most two git calls; :130-137 clamps each per-call timeout to Math.min(GIT_TIMEOUT_MS, floor(remaining budget)) using the caller-supplied budgetMs; :46-50 strips GIT_* env so the identity cannot be redirected. WINDOWS_DRIVE at :31/:75 makes C:\path\repo fall through to the common_dir path. No stubs, TODOs or hardcoded returns; wired into src/capture.ts (:910, :941, :956, :963, :1031) and src/injection/inject.ts:560. Tests cover the credential-bearing remote (test/unit/repo-identity.test.ts:50), the two-call bound (:168) and the integer-timeout case (:182).
- T024: Interpretive: the 543-line test file is real behavioural coverage, not a stub — it imports the actual src/privacy/{detect,classify,egress}.ts and src/config.ts, and every clause of the task line has a matching assertion: corpus-wide redaction + secret classification (line 90) with an 8-char-run leak check (assertNoSecretRun, line 76), stripPrivate incl. unclosed tag / nested / case-and-space-tolerant tags (line 125) and a private span never reaching the detector result (line 145), path-rule hit stored as metadata only with text === '' (line 225), detector throw → {ok:false, reason:'detector_error'} carrying no text (line 241), malformed .oboete.toml → loadRepoRules throws RepoConfigError so capture cannot classify (line 270), and fail-open cases (lines 105/116) asserting byte-identical eligible content. The corpus is the fixture the tests read rather than restating (37 lines, matching the done note). Layer 4 is not_applicable: the only files T024 owns are a test file and a JSONL fixture, and the fixture is read at test/unit/privacy.test.ts:34.
- T025: Interpretive: every clause of the task is genuinely implemented and connected, no stubs. stripPrivate (FR-019, unclosed tag drops the rest); matchSecretPath/compileGlob implement gitignore-style path rules via the linear token sweep the done note describes (Uint8Array reachability, no backtracking RegExp); redactSecrets calls @secretlint/core lintSource with a statically imported recommend preset registered rule-by-rule (filter-comments dropped, AWS enableIDScanRule on) and emits `[REDACTED:${span.rule}]`; entropy is gated behind CANDIDATE_PATTERNS + isHighEntropySecret (length/UUID/alpha/hex guards). detectInWorker spawns a real worker_threads Worker with a setTimeout hard cutoff returning {ok:false,reason:'deadline'} and terminate(); the bundle-side entry detectorWorkerMain is dispatched from src/cli.ts:97 under workerData.role === 'oboete-detector', and src/capture.ts:1189 passes the bundle path as workerScript. DetectorInput.fields for T027 is consumed at src/capture.ts:978-999 with a length-mismatch fail-closed branch. detectSync's catch fails closed to detector_error and never returns unsanitized text.
- T026: Interpretive: src/events.ts:93-149 is a real zod discriminatedUnion over all 10 kinds with `prompt_id: identifier.optional()` spread into every member (the post-review change the done note describes). eventIdKey (src/events.ts:209-223) puts `event.kind` in `base` for every branch, uses no delivery counter and no timestamp, and falls back to turnKey(prompt_id | turnOrdinal | '') + content hash; eventId(event, turnOrdinal) is its sha256 (src/events.ts:232-234) and is called from src/capture.ts:333,356 with the turn ordinal. conversationPolicy (src/events.ts:261-271) implements the documented table: compact=reuse_root, fork/new=new_root, startup/clear/resume/none=by_session_id (resume keeps the root only for a known native session id, which is exactly how Grok --fork-session — source "load" with a new id — becomes a new root), unknown agent always new_root; called from src/capture.ts:415. The residual-limit notes the done line promises are present in the eventIdKey doc comment (src/events.ts:193-208) and mirrored in contracts/agents.md:56-73. No stubs, TODOs or hardcoded returns.
- T027: Interpretive: capture.ts implements the full described path, not a stub. The absolute deadline is measured from process start (CAPTURE_DEADLINE_MS=300, remaining() = deadlineMs - deps.elapsedMs()), SPOOL_RESERVE_MS=40 is held back before the database is opened at all (src/capture.ts:780-782), busy timeout is Math.min(150, floor(remaining - reserve)) with a floor of 1 (src/capture.ts:783-786), git's budget subtracts reserve + row-build margin + the detector's 60 ms slice (src/capture.ts:143-149), the detector runs (runDetector) before any persist() call on every branch (src/capture.ts:952-999), rows go into raw_events via storeRows inside transactionImmediate with a spool fallback on failure (src/capture.ts:571, 806-820, 850), the spool is write-then-rename with a uuid temp suffix (src/spool.ts:65-72), and the worker is spawned only when stored.trigger and isLeaseFree (src/capture.ts:831-834). An unwritable data directory is swallowed and the process still exits 0 (src/capture.ts:866-871). Session reopen on resume is real (reopenSession, src/capture.ts:405, called at 559). cli.ts dispatches hook -> runHook and capture -> runCapture, and both parse --agent/--event, with runCapture additionally parsing --invocation and doing the .started/.done ack rename (src/capture.ts:1244-1310).
- T028: Interpretive: each adapter maps real fixture-derived tool tables (CLAUDE_TOOLS/PI_TOOLS etc.) to normalized events with per-event branches, returns metadataOnly(..., 'unmapped_payload') for tools without a fixture, and adapt() is the single entry point called from src/capture.ts:933 with scanPartialPrefix at src/capture.ts:1029. Done-note claims verified: adapt() cross-checks the payload's declared hook name via sameEventName (src/agents/index.ts:340-345, mismatch → payload_invalid), and scanPartialPrefix unescapes JSON escapes so Windows paths are recovered (index.ts:84-86, 309), covered by test/unit/agents.test.ts:1207. The secret-plant test (test/unit/agents.test.ts:851-918) asserts across all four agents that the serialized unmapped output contains neither the planted path nor the body. No TODO/stub/hardcoded return in src/agents.
- T029: Interpretive: egress.ts really reads the seeded destination_rules table (0002_memory_search.sql:102 DDL + :114 seed) and evaluates class + same-repo scope in one function (isAllowed:44-59), with a fail-closed secret guard; filterEgress:62-84 splits rows and attributes the reason. classify.ts promoteSensitivity:26-40 implements the documented promotion table (private/secret unchanged, non-done unchanged, detector failure unchanged, secret found → secret, otherwise local_only/eligible → eligible), with entropy supplied upstream by detect.ts (entropySpans:263, gated per R4 at detect.ts:251). Both are wired into production paths: src/worker/batches.ts:436-438,487-496,552, src/worker/observe.ts:393, src/observer/classify.ts:259-261,339,598, src/observer/request.ts:67,148. SC-006 is asserted non-tautologically at test/unit/privacy.test.ts:489-528 — a compile-time type assertion that EgressRow has no agent key, plus runtime equality of detector output, promotion and egress decisions across claude/grok. No stubs, TODOs or hardcoded returns.
- T030: Interpretive: lease.ts implements the described behaviour fully and is wired into production paths. claimLease/releaseLease run inside transactionImmediate (BEGIN IMMEDIATE/COMMIT/ROLLBACK, src/worker/lease.ts:23-37); staleness is 6 s old or 60 s future (lease.ts:6-16, STALE_AFTER_MS/FUTURE_SKEW_MS matching the done note); fencing is by owner_token in the UPDATE ... WHERE id=1 AND owner_token=? predicate (lease.ts:68, 86) with assertLease doubling as heartbeat; releaseLease is atomic and checks the empty queue via the queueIsEmpty callback, returning 'kept' when non-empty and 'lost' on token mismatch (lease.ts:82-90). Callers: src/worker/observe.ts:55,682,700,633; src/observer/reservation.ts:57; src/observer/classify.ts:277,485; src/worker/purge.ts:43,76; src/capture.ts:831 (isLeaseFree gating hook spawn). worker_lease row is seeded in src/db/migrations/0001_core.sql:100-109. Tests cover both named cases: lease steal after 6001 ms with the old token fenced out (test/unit/lease.test.ts:56-71) and clock jump at 61 s future stolen / 59 s future not stale (lease.test.ts:73-98), plus release semantics and a SQLITE_BUSY claim path. Minor note (not a defect): STALE_AFTER_MS and FUTURE_SKEW_MS are exported but referenced only inside lease.ts:16.
- T031: Interpretive: batches.ts (656 lines) implements each claimed behaviour with real SQL, not stubs. recoverSpool (src/worker/batches.ts:303) walks the spool in name order, uses INSERT OR IGNORE on the hook-computed raw_events.id for idempotency, derives session/turn parents itself via ensureSessionRows, and quarantines both unparseable files and entries the database refuses (catch at :358, re-throwing SQLITE_BUSY). createBatches (:546) writes one batch per (session, through_event_id, destination) and, when a finished batch already holds that range, derives a distinct through key `${throughEventId}:${id}` (:611-614) — review finding 7. Trigger selection (:504-512) returns session_end for ended sessions, retention for rows past expires_at, and ten_turns once ten distinct turns hold unbatched summarizable rows; retention rows are detached from stale pending batches (:526) and forced to the fallback (:490). reclaimStale (:656) reclaims running batches older than RECLAIM_AFTER_MS = 120_000 (:671). paths.spoolFailed is a real directory created in src/paths.ts:34/45 and used by the quarantine rename in src/spool.ts:101-102. Every exported function is called from src/worker/observe.ts (:749 recoverSpool, :768 createBatches, :757 reclaimStale, :365 loadBatchInput, imports at :41-47); RECLAIM_AFTER_MS and TEN_TURNS are exported but consumed inside their own module (:512, :671), which is not dead code. test/unit/batches.test.ts:171 is exactly the asserted case — "a remote preset splits a mixed session into disjoint remote and fallback batches" — alongside tests for session-end/retention triggers (:260, :280), the second-round through key (:299), spool idempotency and both quarantine paths (:343, :419), and 120 s reclaim (:474).
- T032: Interpretive: purge.ts implements all four claimed behaviours with real bodies, no stubs. Bounded deletes: DELETE_EXPIRED (src/worker/purge.ts:20-29) deletes expired raw_events joined to batches in state applied/fallback, plus failed/secret rows and unbatched rows no summarizer can take (review finding 5), driven by a LIMIT loop in purgeExpiredEvents (:32-52) under an assertLease/transactionImmediate guard that returns leaseLost. pi-ack cleanup: cleanupPiAck (:104-149) folds .done files, records pi_child_hang diagnostics past 30s via recordHangs, removes .started past 24h, ENOENT-tolerant. Checkpoints: checkpoint(db, 'PASSIVE'|'TRUNCATE') (:151) is called PASSIVE per applied batch (src/worker/observe.ts:821) and TRUNCATE on release (observe.ts:880, 894), matching the done note. Forced fallback for expired pending rows correctly lives in batches.ts as the note states (src/worker/batches.ts:490-506, 526-536) and runs before purge in the worker loop. Every export is imported and used outside its definition site (observe.ts, injection/deferred.ts, observer/catalog.ts) and covered by test/unit/purge.test.ts.
- T033: Interpretive: contract.ts implements the full pipeline the done note describes — rawOutputSchema (lenient, type-only) -> trimObservation (applies title/body/citation caps deterministically) -> observerOutputSchema (strict) inside validateObserverOutput, plus post-checks that source_event_ids must be a subset of input.events ids, unknown nearby target rewritten to add/null, and delete-without-reason downgraded to noop. excerptInput enforces MAX_INPUT_CHARS=12,000 with the keep order from contracts/observer.md (tool events dropped, nearby bodies capped to 500, prompts shortened but never below MIN_PROMPT_TEXT=200, free summaries last) and a lastResort pass. No stubs, TODOs, or hardcoded returns; every consumer (llm.ts, providers.ts, request.ts, fallback.ts, classify.ts) is wired to it.
- T034: Interpretive: src/observer/request.ts:117 buildObserverRequest is the sole assembler — every row and every nearby candidate passes the same isAllowed(destination_rules) check (request.ts:54-67, 138-153), free_summaries are filled from admitted rows only, repo_ref is the opaque repoId (request.ts:163), tool_call rows re-emit their command/edit text via toolInputOf (request.ts:88-97, batches.ts:176), and the result is schema-parsed then excerpted. src/worker/observe.ts:386 is the only caller and feeds the result into providerCall. The consent re-check is real but lives one layer out: observe.ts:287 passes consentOk = consentMatches(config, env), and src/observer/llm.ts:412/419/322 evaluate it before reservation, before the provider call, and before the agent-cli child process. No stub, TODO, or hardcoded return. test/unit/request.test.ts:253 asserts the actual serialized body of a mixed batch (eligible text present; local/private/secret/partial markers absent; nearby filtered; repo identity and cwd absent).
- T035: Interpretive: behaviour is genuinely implemented and wired. providers.ts holds resolveModel (empty-model -> ProviderConfigError 'model_required'), createLanguageModel (workers-ai via workers-ai-provider createWorkersAI; ollama/nim/openrouter/gemini via @ai-sdk/openai-compatible createOpenAICompatible with per-preset baseUrl; agent-cli rejected as 'unsupported_preset'), providerRequestOptions (json_schema / response_format / text-json per PRESET_CATALOG.structuredOutput), and runAgentCli spawning `claude -p --output-format json`, `codex exec --json --output-last-message`, `grok -p --output-format json` with AbortSignal.timeout and provider env vars stripped from the child. llm.ts:19-20 sets REQUEST_TIMEOUT_MS=60_000 and MAX_RESPONSE_BYTES=1024*1024; llm.ts:62-67 is the classification table (429/3036 provider_exhausted, 429/3040 provider_exhausted+retry, 403/5035 provider_paid, 3007 unreachable+retry, 401/403 auth_failed); llm.ts:430 passes maxRetries:0; llm.ts:177-215 responseWithinLimit enforces the 1 MB cap on the HTTP envelope by content-length and by streamed byte count; llm.ts:439-445 compares result.response.modelId against the configured model (normalizeRuntimeModelId) and fails 'model_alias'; llm.ts:234-252 neuronsFrom reads cf-aig-neurons/cf-neurons headers and otherwise estimates from token usage. No anthropic preset exists in src/config.ts PRESET_CATALOG (A19 honoured), and agent-cli is capped:false (uncapped). Both exported entry points are consumed by src/worker/observe.ts:33-34,282,717 and covered by test/unit/providers.test.ts and test/unit/llm.test.ts. No TODO/stub/hardcoded return found.
- T036: Interpretive: reserveAttempt (reservation.ts:43-119) runs inside transactionImmediate (lease.ts:24 = BEGIN IMMEDIATE), asserts the lease, refuses on exhausted_at with reset_at in the future, sums calls across capped presets (cappedCalls, CAPPED_PRESETS from PRESET_CATALOG.capped) and refuses at DAILY_CAP=150 or at DAILY_CAP-SESSION_END_RESERVE for non-session_end triggers, then increments provider_usage.calls and observation_batches.provider_attempts/last_reservation_id with state='running' — no stubs. recordExhausted (121-133) is unfenced (no assertLease) and monotonic via COALESCE. llm.ts:137 sets exhaustedSignal only for 429/3036 and llm.ts:481 calls ctx.onExhausted, which observe.ts:304 wires to recordExhausted; observe.ts:289 wires reserve. Fenced apply ends at state 'applied' (observe.ts:461). Tests cover 150 allowed/151 refused, cross-preset sum, uncapped exclusion, session-end reserve, exhausted_at gating, monotonic/idempotent recordExhausted plus lease_lost, and attempt counters.
- T037: Interpretive: fallback.ts implements the four deterministic record rules exactly as contracts/observer.md §"Rule-based fallback" specifies (change per modified turn with 40-line cap + "... (+N omitted)"; bugfix = tool_failure + same-tool successful retry in turn; discovery = failure with no retry; decision = last_assistant_message/compaction_summary first sentence/paragraph verbatim). source_event_ids are derived per rule from the contributing events (sourceIds, capped at MAX_SOURCE_EVENT_IDS=50); budget is MAX_OBSERVATIONS=20 with trim order decisions → changes → bugfixes → discoveries, plus per-field caps via trimObservation. Tombstone/active content_hash matching yields suppressed / noop as specified, never update/delete. No agent name is produced (test asserts /\b(claude|codex|grok|pi)\b/iu absent) and secret-sensitivity events plus partial-classification text are dropped. Wired into production at src/worker/observe.ts:324. Tests cover long paths (line 28, shortened display path), 60 tool calls (line 221), and language of copied text (line 214, Japanese sentence/paragraph boundaries). No stubs, TODOs, or hardcoded returns.
- T038: Interpretive: applyObservations (classify.ts:223) runs the whole apply in transactionImmediate with a lease assert; target restriction filters nearby by repo_id and offered-id set (l.229-231, 293); tombstone suppression queries memories by content_hash and suppresses when deleted_at is not null (l.328-333); sensitivity on add = strictest(detectorClass, every source row) (l.338) and on update additionally maxes in the target row's sensitivity (l.353), with the target-gone case demoting update to add; delete requires a non-empty reason else noop (l.304); directive-corpus rejection via rejectsDirectives on title and body (l.247); checkLanguage is consumed in src/worker/observe.ts:413-437 for exactly one retry then fallback with 'language_mismatch'. Named tests both present: "an eligible update never relaxes a local-only target, in the row and in the next request" (test:434) and "an English answer to a Japanese input is a language mismatch" (test:652), plus the directive corpus test reading test/corpus/directives.jsonl (test:623). No stubs or TODO returns. Discrepancy note (not a negative): the one-retry-then-language_mismatch wiring lives in src/worker/observe.ts, not classify.ts, which only exports the checkLanguage predicate — consistent with the done note's framing.
- T039: Interpretive: runObserve (src/worker/observe.ts:641-927) is a real 280-line loop, not a stub: recoverSpool -> classifyPending -> reclaimStale -> purgeExpiredEvents -> cleanupPiAck -> createBatches -> (workers-ai catalog refresh) -> adoptPendingBatches/processBatch -> pendingSummaries/sessionSummary -> releaseForExit + checkpoint, under a fenced lease with heartbeat and ownsLease re-checks at every step. Exit codes match contracts/cli.md:15 exactly — 0 normal/lease_lost/max_run, 1 when usedFallback (observe.ts:889-891, 925), 3 only for isStorageError (observe.ts:882-884); the done note's claim that a constraint violation is a worker error rather than exit 3 is implemented at isStorageError (observe.ts:109-128) and pinned by the test at test/unit/observe.test.ts:229. The done note's two named deviations are visible in the code: the workers-ai catalog refresh (observe.ts:770-781) runs inside the pass but its result store sits outside the lease fence (flagged as follow-up), and queueIsEmpty (observe.ts:586-613) is an indexed LIMIT 1 probe rather than a row scan. Session-summary reconciliation lives in sessionSummary (src/observer/classify.ts:478-600) rather than in observe.ts — a discrepancy against the task line's wording, not a gap: it targets summary_state='pending' only (classify.ts:494), writes no_content for a session with zero summarizable rows (classify.ts:519), and resolves degraded precedence via DEGRADED_PRECEDENCE over the session's batches (classify.ts:562-568); observe.ts:844-861 drives it. Wired to the CLI at src/cli.ts:30. Tests cover the required case ("a fully private session becomes no_content and is not revisited", observe.test.ts:431) plus 14 others including lease theft, exhaustion fallback, consent mismatch and maxRunMs release. No TODO/stub/hardcoded return found.
- T040: Interpretive: src/observer/catalog.ts genuinely implements the behaviour — it reads Workers AI credentials, serves a cached entry from runtime_state key `workers_ai_catalog` only when the cached `accountId` matches and `now - fetchedAt < 24h` (CACHE_MS = 24*60*60*1000), otherwise calls `GET /accounts/{id}/ai/models/search?per_page=100` with a 10s AbortSignal timeout, validates `success === true`, extracts model names, sets `freeDefaultAvailable` from PRESET_CATALOG['workers-ai'].defaultModel, and derives `paidPlan` from any model property `require_workers_paid` being true/'true'/1, persisting via runtimeStateSet. On fetch/parse failure it falls back to the stale cache rather than throwing. "Once per run" is enforced at the call site: src/worker/observe.ts:772-782 guards with a `catalogChecked` flag, preset === 'workers-ai' and credentials present. No stub, TODO, or hardcoded return. Note: the doctor command that would surface the paid-plan flag is task T069 (still unchecked), so T040 correctly implements only the producing side; `paidPlan` is consumed today by test/unit/catalog.test.ts only.
- T041: Interpretive: fts.ts routes by script run (splitByScript), uses Intl.Segmenter word granularity, sends latin terms >= 3 code points to trigram, emits overlapping CJK bigrams with ー/々/・ accepted as CJK (CJK_CHAR regex incl. halfwidth U+FF70/U+FF65), drops particles/stop words (は が を に で と の …) as run boundaries, skips single-char CJK runs, caps indexed terms at 128 by descending length, and OR-joins double-quote-escaped terms in buildMatch. query.ts runs memories_fts and memories_fts_cjk with bm25() ordering and merges by id, falling back to LIKE (with ESCAPE) only when both matches are null. Both are wired into production (src/injection/pack.ts:611, src/db/queries.ts:278) and covered by 24 tests in test/unit/retrieval.test.ts including seeded English and Japanese memories. No stubs/TODOs.
- T042: Interpretive: rank.ts implements every element of research.md R5 line 111-112 verbatim — best-score normalization of FTS5 BM25 (most-negative = best, ratio score/best), DEFAULT_THRESHOLD 0.3 in applyThreshold, DEFAULT_RRF_K 60 in rrfFuse with isLikeOnly rows forced to score_rrf 0 ("LIKE never votes"), DEFAULT_LAMBDA 0.5 MMR over character-trigram cosine (charTrigramCosine/cosineCounts), and cutToBudget charging title+body+4 for the contracts/agents.md pack prefixes. No stubs or TODOs; every branch has a body. Wired into production: src/injection/pack.ts:633 calls rankCandidates with threshold/lambda 0.5/budget.chars and feeds score_bm25/score_rrf/score_mmr into the assembled pack, and src/db/queries.ts:283 reuses normalizeBm25+rrfFuse. Ten unit tests in test/unit/retrieval.test.ts:405-549 cover each stage plus the LIKE-only and near-duplicate edge cases.
- T043: Interpretive: charBudget() computes Math.floor(min(channelCap, contextFraction × window × CHARS_PER_TOKEN[script])) at src/injection/budget.ts:120-131 — no stub, TODO, or hardcoded return. Windows are parsed at module load from docs/research/context-windows.md through the esbuild '.md': 'text' loader (scripts/build.mjs:22, src/globals.d.ts:8), and the alias rules are derived from the "Runtime id → catalog id" table rows exactly as the done note claims (ALIAS_SUFFIXES, budget.ts:57-65). An unlisted/unreported model falls back to the agent's smallest verified window with known:false, and pack.ts:584/655 turn that into the 'window_unknown' degraded line (reason listed in ledger.ts:19). An agent with no verified window returns blocked:true and pack.ts:583/608 return null, so no injection lane ships. inject.ts:153-154 feeds the real CHANNEL_CAPS entry and config injection.context_fraction, so the budget is on the live path, not test-only.
- T044: Interpretive: pack.ts genuinely builds the pack end to end — `> `-framed canonicalized lines (canonicalLine + `> ` map, pack.ts:150-151), repository line without userinfo, `degraded: ` sentences from DEGRADED_SENTENCES (pack.ts:189), whole-pack secret detector with re-render of survivors and a hard bail (pack.ts:440-455), directive corpus rejection per item (pack.ts:413), control-character assertion (pack.ts:458), ledger rows written as 'planned' and flipped to 'included' on delivery (ledger.ts:144-158), per-epoch uniqueness plus once-per-epoch session-start guard (pack.ts:536-537, alreadyIncluded), summary-pending wait of SUMMARY_WAIT_MS=1s then raw activity (pack.ts:40, 547). Staleness uses fs.existsSync via checkPaths and a HEAD-keyed ancestor cache that clears on HEAD change (staleness.ts:83-86) with `--end-of-options` before the commit (staleness.ts:96). The pack itself reads memories.citations_head/citations_ok (freshCitations, pack.ts:237-245) and calls repositoryHead at most once, only when a commit is cited and within remainingBudget (pack.ts:356-357), matching review finding 10. Consumers exist (inject.ts, deferred.ts, worker/observe.ts) and test/unit/pack.test.ts covers malicious title/path/remote, the `{` guard, stale path and commit, epoch uniqueness, secret detector, directives and budget cuts. No stubs or TODOs.
- T045: Interpretive: the five rules the task describes are genuinely implemented and wired. src/injection/deferred.ts holds the whole state machine: storePending merges into a live pending record (planMerge, whole-pack re-validation via packRejection before commit, pending pack text stored in runtime_state per conversation), attachOnPreToolUse attaches on every PreToolUse until delivered and appends an attempt to attempts_json, confirmOnPostToolUse/deliver sets execution ran|failed, raises delivery_count per probe result (deferred.ts:497), ignores unattached calls once emitted (deferred.ts:471-473) and omits unrendered items (omitUnrendered) so delivery marks only rendered items, markFailure treats PostToolUseFailure as delivered and PermissionDenied as recorded-but-undelivered (record stays open for the next PreToolUse), and closeOnStop drops still-pending attempts even on an emitted record and omits planned items with no_tool_call / not_delivered per sawAnyToolHook (deferred.ts:531-541). Every mutation runs inside transactionImmediate (src/worker/lease.ts:23-29 = BEGIN IMMEDIATE/COMMIT). All five exports are called from the Grok hook path in src/injection/inject.ts:395-430, so the machine is connected, not a stub. test/unit/deferred.test.ts (18 tests) covers all six named cases: success, execution-failure, oboete-deny, other-handler-deny (chain stopped by another handler), parallel-batch, no-tool.
- T046: Interpretive: injection is genuinely implemented and wired end to end, not stubbed. src/cli.ts:31 dispatches `inject` to runInject (src/injection/inject.ts:534), which parses `--agent pi --kind start|prompt`, rejects other combinations, reads stdin against piInjectInputSchema, applies a 1,300 ms Pi session-start deadline (inject.ts:41,557) and writes the pack as plain stdout (inject.ts:638). The hook branches run through capture.ts:757 -> injectForHook (inject.ts:206), which fans out to injectClaude (plain text via immediate() with no envelope, and the startup/clear/compact source gate at inject.ts:301 matching contracts/agents.md:79), injectCodex (envelope() emitting hookSpecificOutput.additionalContext at inject.ts:237-240, including the A18 new-session-id path at inject.ts:329-336) and injectGrok (deferred state machine over PreToolUse/PostToolUse/failure+deny/Stop, inject.ts:394-430). Directive gating uses the observer's normalized rejectsDirectives from src/observer/classify.ts:143, imported by pack.ts:16 and deferred.ts:9, as the done note claims. Failure paths log and return empty/zero rather than throwing.

## Verdict lines

| T012 | ✅ VERIFIED | package.json (name oboete, bin, engines >=22.16, build/typecheck/lint/test), tsconfig.json, eslint.config.js and populated src//test/ skeletons all present and coherent; two deviations are disclosed in the done note (files whitelist instead of .npmignore; pack-check deferred to T087). |
| T013 | ✅ VERIFIED | scripts/build.mjs exists, is in the diff, and actually produces dist/oboete.mjs plus 31 build/test files; bundling/external split and embedded SQL migrations confirmed by inspecting the built bundle. Viewer-asset embedding is explicitly deferred to the still-open T079. |
| T014 | 🔍 PARTIAL | CI matrix (22.16.0 / 24.x) with typecheck, lint, build, test exists in .github/workflows/ci.yml plus a coverage/lcov + Sonar job; the task line's fifth step, pack-check, is absent from CI and from package.json scripts — it is deferred to the still-open T087. |
| T015 | ✅ VERIFIED | Both files exist, are in the diff, and contain a genuine measurement harness plus recorded R13 cold-start and installed-size rows (both pass). Noted deviations: v22.23.1 used as a documented proxy for 22.16, and hook paths judged at 300 ms rather than the task line's 100 ms. |
| T016 | ✅ VERIFIED | 0001_core.sql exists in the diff, defines all nine required tables including the seeded worker_lease row and the four extra sessions columns, and is applied by src/db/open.ts as migration version 1. |
| T017 | ✅ VERIFIED | 0002_memory_search.sql exists, is in the diff, and contains every named object (memories with material_hash/content_hash/review_state, memory_sources, memories_fts trigram, memories_fts_cjk, all six triggers, seeded destination_rules), registered as migration v2 in src/db/open.ts. |
| T018 | ✅ VERIFIED | 0003_operations.sql exists, is in the diff, defines injections (with context_epoch/attempts_json/delivery_count), injection_items with the partial unique index, provider_usage and sync_conflicts, and is registered as migration version 3 in src/db/open.ts. |
| T019 | ✅ VERIFIED | src/db/open.ts fully implements the timeout/WAL/wal_autocheckpoint=0 hook mode, the forward-only sha256-checked migration runner, and the schemaBehind signal that routes hook writes to the spool; all exports are referenced by src/ and test/. |
| T020 | ✅ VERIFIED | Migration smoke tests exist and are real: test/migrations/apply.test.ts covers the empty DB and the version-1 fixture (plus hook/mismatch/ahead/busy paths), the fixture is committed at user_version 1, and CI runs the file on Node 22.16.0 and 24.x. 8/8 pass locally. |
| T021 | ✅ VERIFIED | identity.ts (materialHash/contentHash/memoryIdFor/normalizeForIdentity) and queries.ts memoryScope are fully implemented, live-read destination_rules, fail closed, and are called from injection/observer/worker; queries.test.ts covers cross-repo, secret, and imported exclusion. |
| T022 | ✅ VERIFIED | All three named files exist, are in the diff, and carry every named symbol. No dead symbols (all exports referenced from src/ or test/). Semantics genuinely implemented: OBOETE_HOME, smol-toml config, .oboete.toml bounds, OBOETE_*-only credentials, 5-field consent hash, pause check before DB open. |
| T023 | ✅ VERIFIED | repo-identity.ts implements normalized-remote or git-common-dir realpath identity with sha256 16-hex id; both files in the diff, symbols consumed by capture.ts and inject.ts, tests cover the credential remote and two-call bound. |
| T024 | ✅ VERIFIED | T024's test file and secrets corpus both exist, are in the diff, and every task clause has a real assertion against the actual src/privacy modules. The stdin/1 MB clause is legitimately absent, deferred to T027 per the done note. |
| T025 | ✅ VERIFIED | src/privacy/detect.ts implements all named behaviours (private strip, compileGlob path rules, secretlint lintSource with static recommend preset, gated entropy, [REDACTED:<rule>], worker_threads cutoff, per-field redaction) and is wired into capture.ts, cli.ts, observe.ts with unit tests. |
| T026 | ✅ VERIFIED | src/events.ts implements the zod discriminated union, kind-in-every-key event id derivation with optional turnOrdinal, and the conversation-id policy table; all wired into src/capture.ts and covered by test/unit/events.test.ts. |
| T027 | ✅ VERIFIED | T027 fully implemented in src/capture.ts (1316 lines) with src/spool.ts and src/cli.ts wiring; deadline/reserve/busy-timeout/detector-first/spool-rename/lease-gated spawn all present and referenced from tests. Only noted deviation is the stdin bound, which the done note itself declares. |
| T028 | ✅ VERIFIED | All four agent adapters plus the shared adapt()/scanPartialPrefix entry points are implemented in src/agents/, wired into src/capture.ts, and covered by test/unit/agents.test.ts including the unknown-payload path-rule secret test and the escaped Windows path scan. |
| T029 | ✅ VERIFIED | egress.ts (destination_rules evaluation) and classify.ts (promoteSensitivity/strictest) are fully implemented, consumed by worker/observer code, and covered by test/unit/privacy.test.ts including an explicit SC-006 agent-invariance test. |
| T030 | ✅ VERIFIED | src/worker/lease.ts implements claim/heartbeat/release with BEGIN IMMEDIATE transactions, owner_token fencing, 6 s/60 s staleness and an empty-queue atomic release; all symbols are used by observe/classify/purge/capture and test/unit/lease.test.ts covers lease steal and clock jump. |
| T031 | ✅ VERIFIED | Both named files exist, are in the diff, and contain every claimed behaviour: spool recovery with INSERT OR IGNORE plus quarantine, per-(session, through, destination) batching, ten_turns/session_end/retention triggers, 120 s reclaim. All exports are wired into src/worker/observe.ts. |
| T032 | ✅ VERIFIED | src/worker/purge.ts and test/unit/purge.test.ts both exist, are in the diff, implement bounded lease-guarded deletes, pi-ack cleanup and PASSIVE/TRUNCATE checkpoints, and every export is consumed by observe.ts / deferred.ts / catalog.ts. |
| T033 | ✅ VERIFIED | src/observer/contract.ts implements all zod schemas and every cap named in T033, with lenient-parse -> trim -> strict-validate and excerptInput(12,000 chars); all exports are consumed by llm/providers/request/fallback/classify and covered by test/unit/contract.test.ts. |
| T034 | ✅ VERIFIED | Single outbound request builder exists, is wired into the observe worker, applies destination_rules to events/free_summaries/nearby, emits opaque repo_ref, and is covered by body-level tests. Consent re-check is implemented in llm.ts/observe.ts rather than request.ts. |
| T035 | ✅ VERIFIED | Both files exist, are in the diff, and contain every named preset, limit, retry/classification code, model-id check and neuron estimate. Symbols are imported by src/worker/observe.ts and exercised by unit tests. No stubs. |
| T036 | ✅ VERIFIED | Reservation/daily-cap logic lives in src/observer/reservation.ts (as the done note says, not observe.ts), is wired into src/worker/observe.ts and src/observer/llm.ts's 3036 handling, and is covered by test/unit/callpolicy.test.ts. All mechanical layers positive. |
| T037 | ✅ VERIFIED | src/observer/fallback.ts implements all four deterministic rules with per-rule source_event_ids, 20-observation budget and trim order, agent-neutral output; called from src/worker/observe.ts:324; test/unit/fallback.test.ts covers long paths, 60 calls, and copied-text language. |
| T038 | ✅ VERIFIED | classify.ts implements the fenced apply transaction with repo-scoped target restriction, tombstone suppression, add/update sensitivity maxing, reasoned delete and directive rejection; both named tests and the directive corpus exist and all exports are consumed by worker/observe.ts and injection. |
| T039 | ✅ VERIFIED | src/worker/observe.ts implements runObserve as a full fenced-lease worker loop with exit codes 0/1/3 per contracts/cli.md, wired into src/cli.ts and covered by 15 tests in test/unit/observe.test.ts including the required fully-private-session case. |
| T040 | ✅ VERIFIED | src/observer/catalog.ts implements refreshWorkersAiCatalog with a 24h runtime_state cache keyed to accountId and paidPlan from require_workers_paid; wired once-per-run in src/worker/observe.ts and covered by test/unit/catalog.test.ts. |
| T041 | ✅ VERIFIED | fts.ts/query.ts implement Intl.Segmenter routing, trigram >=3, CJK bigrams incl. ー々・, particle drop, LIKE-only fallback, and bm25-ordered FTS; tests cover EN+JA seeded rows. BM25 normalization itself lives in rank.ts (T042), as expected. |
| T042 | ✅ VERIFIED | src/retrieval/rank.ts implements normalized BM25 (0.3 threshold), RRF k=60 with LIKE never voting, MMR lambda 0.5 over character-trigram cosine, and a character-budget cut; consumed by injection/pack.ts and db/queries.ts, with 10 dedicated tests in test/unit/retrieval.test.ts. |
| T043 | ✅ VERIFIED | budget.ts implements min(channel cap, context_fraction x documented window) with windows parsed from docs/research/context-windows.md, window_unknown fallback and blocked lane; wired into inject.ts/pack.ts and covered by test/unit/budget.test.ts. |
| T044 | ✅ VERIFIED | staleness.ts and pack.ts both implement the described behaviour and are wired into inject.ts/deferred.ts; test/unit/pack.test.ts covers every named case. Done-note correction (1 s wait, not 8 s) matches SUMMARY_WAIT_MS = 1_000. |
| T045 | ✅ VERIFIED | T045 fully implemented: src/injection/deferred.ts contains the complete Grok deferred state machine (merge, attach, deliver, failure, stop) in BEGIN IMMEDIATE transactions, wired into src/injection/inject.ts, with all six named test cases present in test/unit/deferred.test.ts. |
| T046 | ✅ VERIFIED | T046 fully implemented: `oboete inject --agent pi --kind start/prompt` (runInject) plus Claude/Codex/Grok hook injection branches, 1,300 ms deadline, plain-stdout vs additionalContext rules, and directive gating via the observer's rejectsDirectives. All layers positive. |
