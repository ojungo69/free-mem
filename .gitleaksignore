# gitleaks の誤検出をフィンガープリント単位で無効化する。
# 形式: <commit>:<path>:<ruleID>:<line>
# ルール自体は無効化されないため、他の行・他のコミット・今後のコードには引き続き適用される。
#
# 注意: このファイル自体も走査対象になる。除外したい文字列をコメントに書くと
# そのコメント行が新たな検出になるので、リテラルは引用しないこと。

# 相関キーのテスト用ダミー識別子が generic-api-key のエントロピー閾値
# (3.577820) を超えたもの。秘密情報ではない。
# HEAD ではリテラルを改名済みだが、pull_request のスキャンは PR のコミット範囲
# 全体を対象にするため、導入元のコミットを名指しで除外する。
#
# フィンガープリントは commit SHA を含むので、この branch を rebase すると抑止が外れて
# secrets job が落ちる。rebase したら該当コミットの新しい SHA へ貼り替えること
# （squash merge 後は行ごと不要になる。#48）。
3dfaaac9b41c2642b41c856e7d50dac9ae926b80:harness/continuity/reference-model.test.ts:generic-api-key:1309
